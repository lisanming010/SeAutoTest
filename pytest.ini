[pytest]
addopts = -v -s -n 2 --alluredir=./result --clean-alluredir 
marks = 
    test1
    test2
    test3

log_cli = True
log_cli_level = ERROR
log_cli_format = %(asctime)s [%(levelname)s] | %(filename)s:%(lineno)s | %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S
